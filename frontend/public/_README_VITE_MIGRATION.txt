// Vite uses public/index.html as the entry point, so no changes needed unless you want to customize it.
// If you have CRA-specific placeholders (like %PUBLIC_URL%), replace them with Vite equivalents if needed.
// Example: <link rel="icon" href="/favicon.ico" />
